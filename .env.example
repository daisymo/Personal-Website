# —— Local dev (Vite middleware, server-side only) ——
DEEPSEEK_API_KEY=sk-your-key-here
CHAT_RATE_LIMIT_PER_MINUTE=10
CHAT_RATE_LIMIT_PER_DAY=100
# Optional CORS for local cross-origin tests
# CHAT_ALLOWED_ORIGINS=http://localhost:5173

# —— Frontend (safe to expose in build) ——
# Production (GitHub Pages): point to your Cloudflare Worker URL
# VITE_CHAT_API_URL=https://personal-website-chat.your-subdomain.workers.dev
